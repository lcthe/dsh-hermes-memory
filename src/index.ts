export const name = '@lcthe/dsh-hermes-memory'

export const inject: string[] = []

export function apply(): void {
  // V1 host registrations are added after the storage and tool contracts are implemented.
}
